`example/` は commit hash を指定するテスト． `bundle update` した後に `bundle exec ruby ex.rb` とすることで， `Gemfile` で指定された commit の gem を利用できる．

[Make your own gem - RubyGems Guides](https://guides.rubygems.org/make-your-own-gem/)
